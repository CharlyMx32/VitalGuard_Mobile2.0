import 'package:flutter/material.dart';

class VitalIdForgotScreen extends StatelessWidget {
  const VitalIdForgotScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('V4 - Olvide Contrasena'),
      ),
    );
  }
}
