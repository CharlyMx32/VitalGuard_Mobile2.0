import 'package:flutter/material.dart';

class VitalIdResetScreen extends StatelessWidget {
  const VitalIdResetScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('V5 - Restablecer Contrasena'),
      ),
    );
  }
}
