import 'package:flutter/material.dart';

class VitalIdOtpScreen extends StatelessWidget {
  const VitalIdOtpScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('V3 - OTP 2FA Email'),
      ),
    );
  }
}
