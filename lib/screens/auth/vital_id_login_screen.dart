import 'package:flutter/material.dart';

class VitalIdLoginScreen extends StatelessWidget {
  const VitalIdLoginScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('V1 - Login Vital ID'),
      ),
    );
  }
}
