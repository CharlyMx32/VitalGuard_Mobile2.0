import 'package:flutter/material.dart';

class VitalIdSecurityScreen extends StatelessWidget {
  const VitalIdSecurityScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('V6 - Seguridad Vital ID'),
      ),
    );
  }
}
